/**
 * @brief Create a file named "imu_data.txt" to store the data and writes the
 * initial position, velocity and acceleration values if it does not already
 * exist. If the file exists, the function does nothing.
 */
void CreateFile();